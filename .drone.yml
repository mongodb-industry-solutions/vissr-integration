---
kind: pipeline
type: kubernetes
name: staging

trigger:
  branch:
    - staging

steps:
  - name: publish
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/demo-name # TODO: Update with your demo name
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  - name: deploy-staging
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.25.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: demo-name # TODO: Update with your demo name
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/demo-name" # TODO: Update with your demo name
        - "ingress.enabled=true"
        - "ingress.hosts[0]=demo-name.industrysolutions.staging.corp.mongodb.com" # TODO: Update with your demo name
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m" # TODO: Update with required values
        - "resources.limits.memory=1024Mi" # TODO: Update with required values
        - "resources.requests.cpu=250m" # TODO: Update with required values
        - "resources.requests.memory=512Mi" # TODO: Update with required values
      api_server: https://api.staging.corp.mongodb.com
      kubernetes_token:
        from_secret: staging_kubernetes_token
      values_files: ["environments/staging.yaml"]
    when:
      event:
        - push
---
kind: pipeline
type: kubernetes
name: production

trigger:
  branch:
    - main

steps:
  - name: publish
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/demo-name # TODO: Update with your demo name
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  - name: deploy-prod
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.25.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: demo-name # TODO: Update with your demo name
      values:
        - "image.tag=git-${DRONE_COMMIT_SHA:0:7}"
        - "image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/demo-name" # TODO: Update with your demo name
        - "ingress.enabled=true"
        - "ingress.hosts[0]=demo-name.industrysolutions.prod.corp.mongodb.com" # TODO: Update with your demo name
        - "mesh.enabled=true"
        - "resources.limits.cpu=500m" # TODO: Update with required values
        - "resources.limits.memory=1024Mi" # TODO: Update with required values
        - "resources.requests.cpu=250m" # TODO: Update with required values
        - "resources.requests.memory=512Mi" # TODO: Update with required values
      api_server: https://api.prod.corp.mongodb.com
      kubernetes_token:
        from_secret: prod_kubernetes_token
      values_files: ["environments/production.yaml"]
    when:
      event:
        - push
